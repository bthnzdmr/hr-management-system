apiVersion: 1

providers:
  - name: HR
    type: file
    disableDeletion: true
    # Panolar dosyadan gelir ve depoda durur: bir gosterge tablosu da koddur,
    # gozden gecirilebilmeli ve geri alinabilmelidir.
    options:
      path: /var/lib/grafana/dashboards
