# BU DOSYADAKI BUTUN ADRESLER KONTEYNER ICIDIR, tarayiciya yazilacak adres
# degildir. Prometheus panelinin HOST adresi http://localhost:9091 --
# compose "9091:9090" esler, cunku 9090 servislerin yonetim portudur.
# Grafana ise http://localhost:3000. Ikisi de "metrics" profilinde.
#
# Prometheus, servisleri KENDISI kazir (pull). Servisler metrik gondermez.
#
# Bu tercih bilerek: gonderim (push) modelinde bir servis coktugunde susar ve
# "veri yok" ile "sistem saglikli" ayirt edilemez. Cekme modelinde kazima
# basarisiz olur ve BU KENDISI bir sinyaldir (up == 0).
global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Uyari kurallari ayri dosyada: kural da koddur, gozden gecirilebilmeli.
rule_files:
  - /etc/prometheus/rules/*.yml

# Kurallar burada DEGERLENDIRILIR, bildirim Alertmanager'dan gider.
# Bu blok olmadan uyarilar yalnizca /alerts sayfasinda gorunur ve kimseye
# ulasmaz -- tam olarak DLQ'daki 16 mesajin gunlerce fark edilmeme sebebi.
alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]

scrape_configs:
  # Servislerin YONETIM portu (9090) kazilir. O port compose'da disari
  # yayimlanmiyor; Prometheus ayni Docker aginda oldugu icin erisebiliyor.
  - job_name: employee-service
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["employee-service:9090"]
        labels:
          service: employee-service

  - job_name: notification-service
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["notification-service:9090"]
        labels:
          service: notification-service

  # RabbitMQ kendi metriklerini 15692'de sunar (rabbitmq_prometheus eklentisi).
  # DLQ derinligi buradan gelir: tasarim notlari "DLQ'nun tuketicisi yok, elle
  # inceleme yeterli" diyerek riski kabul etmisti -- ama olcum olmadan "elle
  # inceleme", birinin paneli acmayi hatirlamasi demektir.
  - job_name: rabbitmq
    static_configs:
      - targets: ["rabbitmq:15692"]

  # Prometheus kendini kazir. "localhost" BURADA Prometheus konteyneridir;
  # tarayicidan ayni adres calismaz (host'ta panel 9091'dedir).
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Alertmanager da kazinir. Kazinmasaydi COKTUGUNDE kimse fark etmezdi ve
  # sistem "uyari gonderiyorum" sanirken sessiz kalirdi -- uyari altyapisinin
  # kendi arizasi, uyarilarin en sinsisidir. `TargetDown` kurali bunu yakalar.
  - job_name: alertmanager
    static_configs:
      - targets: ["alertmanager:9093"]
